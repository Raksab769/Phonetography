# Phonetography
📸 An aesthetic, browser-based photo analysis studio that reads an image's color profile and matches it to classic presets with live control over filter strength, aspect ratio, and zoom. Built entirely in vanilla HTML/JS with zero dependencies.
